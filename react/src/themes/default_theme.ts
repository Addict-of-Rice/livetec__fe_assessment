import type { ThemeConfig } from './theme';

export const defaultTheme: ThemeConfig = {
  fontFamily: {
    heading: 'system-ui, -apple-system, Segoe UI, Roboto, Arial, Helvetica, sans-serif;',
    body: 'system-ui, -apple-system, Segoe UI, Roboto, Arial, Helvetica, sans-serif;',
  },
  bodySize: 1,
  typeScale: 1.333,
  fontWeight: 500,
};